FILE=Table
Product=nucondb

# p372    -> python v3_7_2   (e17 e19 c2 c7)
# p2715a  -> python v2_7_15a (e17 e19 c2 c7)
# p2714b  -> python v2_7_14b (e17 e15 c2)

group:

Flavor=ANY
Qualifiers=""

  Action=DefineFQ
    EnvSet(NUCONDB_FQ_DIR , ${UPS_PROD_DIR} )

  Action = ExtraSetup
    ##setupRequired( ifdhc v2_5_1 )

Flavor=ANY
Qualifiers=c7:p372:debug

  Action=DefineFQ
    EnvSet(NUCONDB_FQ_DIR , ${UPS_PROD_DIR}/${UPS_PROD_FLAVOR}-c7-p372-debug )

  Action = ExtraSetup
    setupRequired( ifdhc v2_5_1 -q +p372:+c7:+debug )
    setupRequired( clang v7_0_0 )

Flavor=ANY
Qualifiers=c7:p372:prof

  Action=DefineFQ
    EnvSet(NUCONDB_FQ_DIR , ${UPS_PROD_DIR}/${UPS_PROD_FLAVOR}-c7-p372-prof )

  Action = ExtraSetup
    setupRequired( ifdhc v2_5_1 -q +p372:+c7:+prof )
    setupRequired( clang v7_0_0 )

Flavor=ANY
Qualifiers=c2:p372:debug

  Action=DefineFQ
    EnvSet(NUCONDB_FQ_DIR , ${UPS_PROD_DIR}/${UPS_PROD_FLAVOR}-c2-p372-debug )

  Action = ExtraSetup
    setupRequired( ifdhc v2_5_1 -q +p372:+c2:+debug )
    setupRequired( clang v5_0_1 )

Flavor=ANY
Qualifiers=c2:p372:prof

  Action=DefineFQ
    EnvSet(NUCONDB_FQ_DIR , ${UPS_PROD_DIR}/${UPS_PROD_FLAVOR}-c2-p372-prof )

  Action = ExtraSetup
    setupRequired( ifdhc v2_5_1 -q +p372:+c2:+prof )
    setupRequired( clang v5_0_1 )

Flavor=ANY
Qualifiers=c7:p2715a:debug

  Action=DefineFQ
    EnvSet(NUCONDB_FQ_DIR , ${UPS_PROD_DIR}/${UPS_PROD_FLAVOR}-c7-p2715a-debug )

  Action = ExtraSetup
    setupRequired( ifdhc v2_5_1 -q +p2715a:+c7:+debug )
    setupRequired( clang v7_0_0 )

Flavor=ANY
Qualifiers=c7:p2715a:prof

  Action=DefineFQ
    EnvSet(NUCONDB_FQ_DIR , ${UPS_PROD_DIR}/${UPS_PROD_FLAVOR}-c7-p2715a-prof )

  Action = ExtraSetup
    setupRequired( ifdhc v2_5_1 -q +p2715a:+c7:+prof )
    setupRequired( clang v7_0_0 )

Flavor=ANY
Qualifiers=c2:p2715a:debug

  Action=DefineFQ
    EnvSet(NUCONDB_FQ_DIR , ${UPS_PROD_DIR}/${UPS_PROD_FLAVOR}-c2-p2715a-debug )

  Action = ExtraSetup
    setupRequired( ifdhc v2_5_1 -q +p2715a:+c2:+debug )
    setupRequired( clang v5_0_1 )

Flavor=ANY
Qualifiers=c2:p2715a:prof

  Action=DefineFQ
    EnvSet(NUCONDB_FQ_DIR , ${UPS_PROD_DIR}/${UPS_PROD_FLAVOR}-c2-p2715a-prof )

  Action = ExtraSetup
    setupRequired( ifdhc v2_5_1 -q +p2715a:+c2:+prof )
    setupRequired( clang v5_0_1 )

Flavor=ANY
Qualifiers=c2:p2714b:debug

  Action=DefineFQ
    EnvSet(NUCONDB_FQ_DIR , ${UPS_PROD_DIR}/${UPS_PROD_FLAVOR}-c2-p2714b-debug )

  Action = ExtraSetup
    setupRequired( ifdhc v2_5_1 -q +p2714b:+c2:+debug )
    setupRequired( clang v5_0_1 )

Flavor=ANY
Qualifiers=c2:p2714b:prof

  Action=DefineFQ
    EnvSet(NUCONDB_FQ_DIR , ${UPS_PROD_DIR}/${UPS_PROD_FLAVOR}-c2-p2714b-prof )

  Action = ExtraSetup
    setupRequired( ifdhc v2_5_1 -q +p2714b:+c2:+prof )
    setupRequired( clang v5_0_1 )

Flavor=ANY
Qualifiers=e19:p372:debug

  Action=DefineFQ
    EnvSet(NUCONDB_FQ_DIR , ${UPS_PROD_DIR}/${UPS_PROD_FLAVOR}-e19-p372-debug )

  Action = ExtraSetup
    setupRequired( ifdhc v2_5_1 -q +p372:+e19:+debug )
    setupRequired( gcc v8_2_0 )

Flavor=ANY
Qualifiers=e19:p372:prof

  Action=DefineFQ
    EnvSet(NUCONDB_FQ_DIR , ${UPS_PROD_DIR}/${UPS_PROD_FLAVOR}-e19-p372-prof )

  Action = ExtraSetup
    setupRequired( ifdhc v2_5_1 -q +p372:+e19:+prof )
    setupRequired( gcc v8_2_0 )

Flavor=ANY
Qualifiers=e17:p372:debug

  Action=DefineFQ
    EnvSet(NUCONDB_FQ_DIR , ${UPS_PROD_DIR}/${UPS_PROD_FLAVOR}-e17-p372-debug )

  Action = ExtraSetup
    setupRequired( ifdhc v2_5_1 -q +p372:+e17:+debug )
    setupRequired( gcc v7_3_0 )

Flavor=ANY
Qualifiers=e17:p372:prof

  Action=DefineFQ
    EnvSet(NUCONDB_FQ_DIR , ${UPS_PROD_DIR}/${UPS_PROD_FLAVOR}-e17-p372-prof )

  Action = ExtraSetup
    setupRequired( ifdhc v2_5_1 -q +p372:+e17:+prof )
    setupRequired( gcc v7_3_0 )

Flavor=ANY
Qualifiers=e19:p2715a:debug

  Action=DefineFQ
    EnvSet(NUCONDB_FQ_DIR , ${UPS_PROD_DIR}/${UPS_PROD_FLAVOR}-e19-p2715a-debug )

  Action = ExtraSetup
    setupRequired( ifdhc v2_5_1 -q +p2715a:+e19:+debug )
    setupRequired( gcc v8_2_0 )

Flavor=ANY
Qualifiers=e19:p2715a:prof

  Action=DefineFQ
    EnvSet(NUCONDB_FQ_DIR , ${UPS_PROD_DIR}/${UPS_PROD_FLAVOR}-e19-p2715a-prof )

  Action = ExtraSetup
    setupRequired( ifdhc v2_5_1 -q +p2715a:+e19:+prof )
    setupRequired( gcc v8_2_0 )

Flavor=ANY
Qualifiers=e17:p2715a:debug

  Action=DefineFQ
    EnvSet(NUCONDB_FQ_DIR , ${UPS_PROD_DIR}/${UPS_PROD_FLAVOR}-e17-p2715a-debug )

  Action = ExtraSetup
    setupRequired( ifdhc v2_5_1 -q +p2715a:+e17:+debug )
    setupRequired( gcc v7_3_0 )

Flavor=ANY
Qualifiers=e17:p2715a:prof

  Action=DefineFQ
    EnvSet(NUCONDB_FQ_DIR , ${UPS_PROD_DIR}/${UPS_PROD_FLAVOR}-e17-p2715a-prof )

  Action = ExtraSetup
    setupRequired( ifdhc v2_5_1 -q +p2715a:+e17:+prof )
    setupRequired( gcc v7_3_0 )

Flavor=ANY
Qualifiers=e17:p2714b:debug

  Action=DefineFQ
    EnvSet(NUCONDB_FQ_DIR , ${UPS_PROD_DIR}/${UPS_PROD_FLAVOR}-e17-p2714b-debug )

  Action = ExtraSetup
    setupRequired( ifdhc v2_5_1 -q +p2714b:+e17:+debug )
    setupRequired( gcc v7_3_0 )

Flavor=ANY
Qualifiers=e17:p2714b:prof

  Action=DefineFQ
    EnvSet(NUCONDB_FQ_DIR , ${UPS_PROD_DIR}/${UPS_PROD_FLAVOR}-e17-p2714b-prof )

  Action = ExtraSetup
    setupRequired( ifdhc v2_5_1 -q +p2714b:+e17:+prof )
    setupRequired( gcc v7_3_0 )

Flavor=ANY
Qualifiers=e15:p2714b:debug

  Action=DefineFQ
    EnvSet(NUCONDB_FQ_DIR , ${UPS_PROD_DIR}/${UPS_PROD_FLAVOR}-e15-p2714b-debug )

  Action = ExtraSetup
    setupRequired( ifdhc v2_5_1 -q +p2714b:+e15:+debug )
    setupRequired( gcc v6_4_0 )

Flavor=ANY
Qualifiers=e15:p2714b:prof

  Action=DefineFQ
    EnvSet(NUCONDB_FQ_DIR , ${UPS_PROD_DIR}/${UPS_PROD_FLAVOR}-e15-p2714b-prof )

  Action = ExtraSetup
    setupRequired( ifdhc v2_5_1 -q +p2714b:+e15:+prof )
    setupRequired( gcc v6_4_0 )

common:

Action=Setup
    SetupEnv()
    proddir()
    EnvSet(NUCONDB_VERSION, ${UPS_PROD_VERSION} )
    exeActionRequired(DefineFQ)
    EnvSet(NUCONDB_LIB,     ${NUCONDB_FQ_DIR}/lib)
    if ( test `uname` = "Darwin" )
      envPrepend(DYLD_LIBRARY_PATH, ${NUCONDB_LIB})
    else()
      envPrepend(LD_LIBRARY_PATH, ${NUCONDB_LIB})
    endif ( test `uname` = "Darwin" )
    # requirements
    exeActionRequired(ExtraSetup)
    setupRequired(libwda v2_26_0)
    # useful variables
    envPrepend(CMAKE_PREFIX_PATH, ${NUCONDB_FQ_DIR} )
    envPrepend(PKG_CONFIG_PATH, ${NUCONDB_FQ_DIR} )
end:
